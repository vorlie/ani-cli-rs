feat: add adult search and harden Windows releases

- add -a/--allow-adult to interactive and scriptable searches
- support ANI_CLI_ALLOW_ADULT for persistent configuration
- trust both Windows-native and bundled public certificate roots
- include underlying TLS and transport causes in network errors
- remap local profile paths out of Windows release binaries
